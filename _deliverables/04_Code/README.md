# Code

Moodle artifact: `CMPE492-Code`

Required filenames:

- `121200152_Atakan_Gul_code.zip`
- `122200045_Arda_Kaan_Yildiz_code.zip`

Suggested contents:

- Repository source excluding generated folders such as `web/node_modules`, `web/dist`, caches, local uploads, and build outputs.
- Root `README.md`.
- API/web setup instructions.
- Any required migration, seed, or evaluation scripts needed to reproduce the demo/report.

Before zipping:

- Check `git status`.
- Run the selected verification commands.
- Avoid including secrets or local `.env` files.

